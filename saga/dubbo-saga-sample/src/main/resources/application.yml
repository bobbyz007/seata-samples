spring:
  profiles:
    active: consumer
  main:
    allow-circular-references: true
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test_dubbo_saga?useSSL=false&serverTimezone=UTC
    username: mysql
    password: mysql